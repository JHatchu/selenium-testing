
# selenium-testing-project

Multi-module Maven project for Selenium capstone:
- orangehrm (Selenium + TestNG)
- reqres-api (RestAssured + TestNG + Word docs)
- bmi-cucumber (Cucumber + Selenium)

Run all tests: `mvn test` at project root.
